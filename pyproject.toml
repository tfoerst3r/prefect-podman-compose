[project]
name = "prefectflow"
version = "0.1.0"
description = ""
authors = [
    {name = "tfoerst3r",email = "no-reply@tfoerster.biz.id"}
]
readme = "README.md"
requires-python = ">=3.13, <3.15"
dependencies = [
    "prefect (>=3.8.5,<4.0.0)",
    "psycopg2 (>=2.9.13,<3.0.0)"
]

[tool.poetry]
packages = [{include = "prefectflow", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

